public with sharing class AccountHelper {
    public AccountHelper() {
        system.System.debug('Hello');
        for (int i = 0; i < 42; i++)
            system.System.debug('Hello');
        if(true)
        {
            if(true)
            {
                   if(true)
                   {
                    system.System.debug('Hello');
                   }
            }
        }
    }
}
